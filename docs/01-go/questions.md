# Go — вопросы для самопроверки (без ответов)

Читать вслух, отвечать вслух, как на реальном собесе. Ответы — в `concurrency.md` / `memory-and-types.md`.

## Concurrency

1. Что такое goroutine и чем она дешевле OS-потока?
2. Опишите модель G-M-P своими словами.
3. Что происходит с P, когда его M блокируется на syscall?
4. Что такое work stealing и почему воруется "половина", а не одна goroutine?
5. Что произойдёт при `close(ch)` дважды?
6. Можно ли закрыть receive-only канал? Почему?
7. Что будет с `for v := range ch`, если канал никогда не закроют?
8. Почему при нескольких готовых case в `select` выбор случайный?
9. Как сделать неблокирующий read/write в канал?
10. Почему channel может быть медленнее Mutex? Когда вы сознательно выберете Mutex вместо канала?
11. Когда RWMutex может дать writer starvation?
12. В чём принципиальная разница atomic vs Mutex (не "быстрее/медленнее", а по сути)?
13. Что такое ABA problem?
14. Классическая ошибка использования WaitGroup — какая и почему она рождает race?
15. Когда sync.Map оправдан, а когда — нет?
16. В чём разница cancel / timeout / deadline у context?
17. Почему через context.Value нельзя пробрасывать обязательные зависимости?
18. Что случится с дочерним контекстом, если отменили родительский? А наоборот?
19. Почему map не потокобезопасна — дайте Senior-ответ, а не "там race".
20. Deadlock vs livelock vs starvation — приведите пример каждого.
21. Может ли рантайм Go сам обнаружить deadlock? При каком условии — да, при каком — нет?
22. Как работает `go test -race` технически и какие у него ограничения?
23. Что выведет цикл `for i := 0; i < 3; i++ { go func(){ print(i) }() }` в Go 1.21 и в Go 1.22+? Почему разница?

## Types & Memory

24. Array vs slice — в чём разница на уровне семантики передачи в функцию?
25. Почему append иногда меняет исходный массив, а иногда нет?
26. Как получить slice, изменение которого через append точно не заденет исходные данные?
27. nil slice vs empty slice — где разница проявляется на практике?
28. Как устроена map внутри (bucket, overflow, tophash)?
29. Что изменилось в map в Go 1.24 (Swiss Table) на уровне идеи, а не деталей?
30. Что такое iface и eface, чем отличаются?
31. Почему `itab` кэшируется рантаймом?
32. Объясните ловушку "typed nil в интерфейсе" — почему `err != nil`, хотя переменная-указатель nil?
33. Что такое escape analysis и какое решение он принимает?
33a. Чем стек goroutine отличается от стека OS-потока — с какого размера стартует и что происходит при его росте?
33b. Почему указатель на локальную переменную можно безопасно "передвинуть" при росте стека, но нельзя просто отдать в другую goroutine без escape to heap?
33c. Правда ли, что передача указателя в функцию всегда форсирует escape? Приведите пример, когда нет, и объясните, за счёт чего это возможно (inlining/межпроцедурный анализ).
33d. Дана функция, возвращающая указатель на локальную переменную, и функция, возвращающая значение того же типа по значению — почему первая форсирует heap, а вторая может остаться на стеке?
33e. Почему `make([]int, n)` с переменной `n` почти гарантированно уходит в heap, а `make([]int, 4)` может остаться на стеке?
33f. Почему передача значения в `interface{}`-параметр (например, `fmt.Println(x)`) — частый источник неожиданных heap-аллокаций?
34. Почему указатели чаще форсируют escape to heap, чем значения (в каких конкретно случаях, а не "вообще")?
35. Как посмотреть через `-gcflags`, что ушло в heap, а что осталось на стеке — какая разница между `-m` и `-m -m`?
36. Почему `[]byte` иногда предпочтительнее `string` в hot path?
37. Как в общих чертах устроен GC в Go (алгоритм, конкурентность, триггер)?
38. Как GC влияет на latency и что с этим можно сделать на уровне кода?

## Практика "что выведет / где ошибка"

39. Дан код с горутиной, читающей и пишущей map без синхронизации — что произойдёт при запуске?
40. Дан код с двумя горутинами, берущими два мьютекса в разном порядке — что произойдёт?
41. Дан код, где `wg.Add(1)` вызван внутри самой goroutine — в чём баг?
42. Дан код с send в уже закрытый канал — что произойдёт?
43. Дан код, где слайс передаётся в функцию, та делает `append` и не возвращает результат — увидит ли вызывающий код изменения?
