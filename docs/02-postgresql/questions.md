# PostgreSQL — вопросы для самопроверки (без ответов)

Читать вслух, отвечать вслух. Ответы — в `indexes-and-planner.md` / `transactions-and-mvcc.md`.

## Индексы и планировщик

1. Когда B-tree не подходит и почему? Чем его заменить для full-text/jsonb? Для геоданных? Для time-series логов?
2. Почему planner может проигнорировать существующий индекс и выбрать Seq Scan?
3. Какие две принципиально разные причины у "индекс не используется" — теоретическая (cost-based выбор) и практическая (что именно в проде)?
4. Почему `LIKE '%abc'` не использует обычный B-tree, а `LIKE 'abc%'` — использует? Как ускорить поиск по подстроке в любом месте строки?
5. Почему `COUNT(*)` в PostgreSQL дорогой? Как получить быструю оценку количества строк без точного счёта?
6. В чём разница INNER JOIN и LEFT JOIN с точки зрения свободы planner'а в выборе порядка соединения?
7. Назовите три алгоритма JOIN и когда planner выбирает каждый.
8. В чём разница EXPLAIN и EXPLAIN ANALYZE? Почему EXPLAIN ANALYZE на UPDATE/DELETE опасен на проде?
9. На что смотреть в выводе EXPLAIN ANALYZE в первую очередь при диагностике медленного запроса?
10. Почему OFFSET-пагинация медленная на больших страницах и почему её результат может "плавать" при конкурентных изменениях?
11. Как устроена cursor/keyset-пагинация и что делать, если ключ сортировки не уникален?

## MVCC и транзакции

12. Что такое MVCC простыми словами — что происходит физически при UPDATE строки?
13. Почему таблица растёт в размере, даже если строки только обновляются, а не добавляются?
14. Что делает обычный VACUUM и почему он не уменьшает размер файла на диске? Чем отличается VACUUM FULL и почему он опасен на проде?
15. Что такое HOT update и при каком условии он происходит?
16. Опишите разницу READ COMMITTED / REPEATABLE READ / SERIALIZABLE на уровне снапшотов (не просто через названия аномалий).
17. Что такое write skew и почему REPEATABLE READ его не устраняет?
18. Как PostgreSQL реализует SERIALIZABLE технически (SSI) — блокировочно или оптимистично? Что должно делать приложение при ошибке сериализации?
19. Что такое deadlock, как PostgreSQL его обнаруживает и как правильно проектировать код, чтобы снизить его вероятность?
20. Когда обязательно нужен `SELECT ... FOR UPDATE`? Приведите пример бага без него (lost update).
21. Что делает `FOR UPDATE SKIP LOCKED` и для какого паттерна (очередь задач) это классическое решение?
22. Как работает `INSERT ... ON CONFLICT DO UPDATE` и почему это безопаснее, чем "проверить — потом вставить"?
23. Как через `ON CONFLICT` инкрементировать значение с учётом старого и нового?
